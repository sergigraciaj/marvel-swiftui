import SwiftUI

struct ErrorView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("ErrorView")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
